[package]
name = "multi-account-ai-control-desktop"
version = "0.1.0"
description = "Desktop wrapper baseline for Multi Account AI Control"
authors = ["MAAC Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
